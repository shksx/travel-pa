// Firebase project config.
// Replace the placeholder values below with the firebaseConfig object you copied
// from Firebase console → Project settings → Your apps → Web app (nomad-web).
//
// These keys are PUBLIC — they identify the project, not authenticate it.
// Real security comes from Firestore security rules + Authorized domains.

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
